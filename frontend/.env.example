# Frontend environment (optional)
# Vite reads this file automatically. If omitted, the app defaults to
# http://localhost:8080, which works for the standard local setup.

VITE_API_BASE_URL=http://localhost:8080
